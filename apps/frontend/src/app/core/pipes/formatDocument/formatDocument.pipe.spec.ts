import { FormatDocumentPipe } from './formatDocument.pipe';

describe('FormatDocumentPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDocumentPipe();
    expect(pipe).toBeTruthy();
  });
});
